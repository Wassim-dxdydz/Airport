spring:
  flyway:
    enabled: false

  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
      data-locations: []

logging:
  level:
    org.springframework.data.r2dbc: DEBUG
    fr.uga.miage.m1: DEBUG
